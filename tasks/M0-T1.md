# M0-T1 — Project Scaffold & Directory Structure

| Field | Value |
|---|---|
| **Task ID** | M0-T1 |
| **Milestone** | M0 — Scaffold |
| **Depends On** | none |
| **Session** | 1 |
| **Status** | done |

---

## Objective

Create the full directory tree and all empty `__init__.py` files for the `memory-layer` project.
Every subsequent task builds on top of this scaffold.

---

## Perplexity Prompt — paste this verbatim

You are scaffolding a production-grade Python project called `memory-layer`.
It is a framework-agnostic AI memory layer built with ports-and-adapters (hexagonal) architecture.
Generate the full directory tree and all empty `__init__.py` files needed.

**Project name:** `memory-layer`
**Python version:** 3.11+
**Package root:** `src/memory_layer/`

**Required directory structure:**

```
memory-layer/
├── src/
│   └── memory_layer/
│       ├── __init__.py
│       ├── domain/
│       │   ├── __init__.py
│       │   ├── types.py
│       │   ├── records.py
│       │   ├── policies.py
│       │   └── events.py
│       ├── ports/
│       │   ├── __init__.py
│       │   ├── inbound.py
│       │   └── outbound.py
│       ├── engine/
│       │   ├── __init__.py
│       │   ├── ingestion.py
│       │   ├── extraction.py
│       │   ├── contradiction.py
│       │   ├── planner.py
│       │   ├── retrieval.py
│       │   └── recall.py
│       ├── policy/
│       │   ├── __init__.py
│       │   ├── consolidation.py
│       │   ├── decay.py
│       │   └── scheduler.py
│       ├── storage/
│       │   ├── __init__.py
│       │   ├── sqlite/
│       │   │   ├── __init__.py
│       │   │   ├── record_repo.py
│       │   │   ├── fact_repo.py
│       │   │   └── migrations/
│       │   │       └── V1__initial_schema.sql
│       │   └── vector/
│       │       ├── __init__.py
│       │       └── local_vector.py
│       ├── adapters/
│       │   ├── __init__.py
│       │   ├── rest/
│       │   │   ├── __init__.py
│       │   │   └── app.py
│       │   ├── sdk/
│       │   │   ├── __init__.py
│       │   │   └── client.py
│       │   ├── mcp/
│       │   │   ├── __init__.py
│       │   │   └── server.py
│       │   └── langgraph/
│       │       ├── __init__.py
│       │       ├── recall_node.py
│       │       └── write_node.py
│       └── observability/
│           ├── __init__.py
│           ├── emitter.py
│           └── audit.py
├── tests/
│   ├── __init__.py
│   ├── domain/
│   │   └── __init__.py
│   ├── engine/
│   │   └── __init__.py
│   ├── storage/
│   │   └── __init__.py
│   ├── adapters/
│   │   └── __init__.py
│   └── benchmarks/
│       └── __init__.py
├── migrations/
│   └── .gitkeep
├── docs/
│   └── .gitkeep
├── .github/
│   └── workflows/
│       └── ci.yml
├── pyproject.toml
├── README.md
└── .gitignore
```

**Deliverables:**
1. Every file listed above, even if empty.
2. Every `__init__.py` contains only a one-sentence module docstring.
3. `README.md` with project name, one-paragraph description, and a "Getting Started" section showing `pip install -e .[dev]` and `pytest`.
4. `.gitignore` for Python: `.venv`, `__pycache__`, `*.pyc`, `.env`, `dist/`, `.pytest_cache/`, `*.egg-info`.

**Do not generate any implementation logic.** Scaffold only.

---

## Acceptance Checks

Run these after applying the generated output:

```bash
# Must return 16 or more
find src -name "__init__.py" | wc -l

# Must succeed
pip install -e .
python -c "import memory_layer"

# Must show the directory tree
ls src/memory_layer/
```

---

## Commit Message

```
chore(scaffold): initialize project directory structure
```
